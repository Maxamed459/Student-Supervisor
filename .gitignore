# Dependencies
node_modules/

# Environment secrets — never commit
.env
.env.local
.env.*.local

# Local uploads (legacy; files now live on Cloudinary)
uploads/
server/uploads/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Build
dist/
build/
